{
  "name": "@agent-coordinator/cli",
  "version": "0.1.0",
  "description": "CLI for Agent Coordinator.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "agent-coordinator": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "check": "tsc -b --pretty false"
  },
  "dependencies": {
    "@agent-coordinator/core": "workspace:*",
    "commander": "^14.0.2"
  }
}
